export type Champ = {
    id?: number,
    name: string;
    role: string,
    lane: string
    difficulty: number,
    damage_type: string,
    blue_essence: number,
    images: [],
    description: string,
}